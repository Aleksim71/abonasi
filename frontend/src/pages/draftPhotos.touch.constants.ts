// frontend/src/pages/draftPhotos.touch.constants.ts

/**
 * Touch/Pointer affordances tuning for mobile reorder UX.
 * Keep these values in one place to avoid magic numbers spread across handlers.
 */

export const LONG_PRESS_DELAY_MS = 350;
export const TOUCH_MOVE_TOLERANCE_PX = 8;

/**
 * Optional: if you auto-hide hint once shown.
 * If your UI keeps the hint until interaction completes, you can ignore this.
 */
export const HINT_AUTO_HIDE_MS = 2000;



> Обновляй этот файл после каждого коммита: отмечай ✅ и добавляй короткую строку “что именно”.

---

## 0) Базовая фиксация проекта

### ✅ Сделано
- [x] Создан репозиторий `abonasi/` со структурой `frontend/`, `backend/`, `docs/`
- [x] Добавлены `README.md`, `.gitignore`
- [x] Зафиксированы правила работы с Codex: `docs/WORKFLOW.md`, `docs/PROMPTS.md`
- [x] Зафиксирован API контракт: `docs/API_CONTRACT.md`
- [x] Включён dev changelog: `docs/CHANGELOG_DEV.md`
- [x] Git bootstrap + тег: `v0.0.0-bootstrap`

### 🔜 Дальше
- [ ] (Опционально) Добавить `docs/DECISIONS.md` (короткие решения по архитектуре)

---

## 1) База данных (PostgreSQL)

### ✅ Сделано
- [x] Создан отдельный пользователь БД: `abonasi_user`
- [x] Создана отдельная база: `abonasi`
- [x] Установлена схема: `backend/sql/DB_SCHEMA.sql`
  - [x] Таблицы: `users`, `locations`, `ads`, `ad_photos`
  - [x] Индексы для фида / моих объявлений
- [x] Seed локаций: `backend/sql/seed_locations.sql` (29 записей)

### 🔜 Дальше
- [ ] (Опционально) Скрипт `seed_dev.sql` (локации + 2–5 тестовых пользователей + 10 объявлений)
- [ ] (Опционально) Миграции (если решим уходить от “одного schema.sql”)

---

## 2) Backend skeleton (Node.js + Express + Postgres)

### ✅ Сделано
- [x] `backend/package.json` + зависимости установлены (`npm i`)
- [x] Сервер запускается `npm run dev`
- [x] `/api/health` работает и проверяет БД (`db.ok: true`)
- [x] Подключение к Postgres через `pg Pool` (`backend/src/config/db.js`)

### ✅ Модуль Locations
- [x] `GET /api/locations`
- [x] Фильтры: `?country=...&city=...`
- [x] Возвращает JSON список районов, сортировка по country/city/district

### 🔜 Дальше (рекомендуемая последовательность)
1. [ ] **Locations cascade endpoints** (для фронта)
   - [ ] `GET /api/locations/countries`
   - [ ] `GET /api/locations/cities?country=Germany`
   - [ ] `GET /api/locations/districts?country=Germany&city=Munich`
2. [ ] **Auth**
   - [ ] `POST /api/auth/register` (bcrypt)
   - [ ] `POST /api/auth/login` (JWT)
   - [ ] `GET /api/auth/me`
   - [ ] middleware `auth` (Bearer token)
3. [ ] **Ads (MVP)**
   - [ ] `POST /api/ads` → создать draft
   - [ ] `POST /api/ads/:id/publish` → active + published_at
   - [ ] `POST /api/ads/:id/stop` → stopped + stopped_at
   - [ ] `GET /api/ads/my` → мои объявления
   - [ ] `GET /api/ads?locationId=...` → фид района
4. [ ] **Photos (MVP заглушка)**
   - [ ] Добавить эндпоинт “привязать фото” (пока без реального S3)
   - [ ] Протокол: хранить `file_path` (позже заменить на storage)

---

## 3) Frontend (пока заглушка)

### ✅ Сделано
- [x] Папка `frontend/` создана

### 🔜 Дальше (после каскадных локаций)
- [ ] Экран выбора локации: Country → City → District
- [ ] Экран “Лента района” (читает `/api/ads?locationId=...`)
- [ ] Экран “Новое объявление” (draft → publish)

---

## 4) Git / Коммиты / Контроль качества

### ✅ Сделано
- [x] Коммиты маленькие и по смыслу
- [x] История читаемая (bootstrap → db → backend → locations)

### 🔜 Дальше
- [ ] Ввести правило: 1 задача = 1 коммит (или серия маленьких)
- [ ] (Опционально) Husky + pre-commit (lint/test позже)

---

## 5) Аудит и периодичность (нужен, но лёгкий)

> Для MVP не надо “безумного” аудита. Нужен простой, регулярный контроль, чтобы не копить техдолг и риски.

### Ежедневно (в дни разработки, 2–5 минут)
- [ ] `git status` — рабочее дерево чистое
- [ ] `npm run dev` запускается
- [ ] `/api/health` возвращает `db.ok=true`

### Еженедельно (15–30 минут)
- [ ] Проверить зависимости backend:
  - [ ] `npm audit` (записать результаты)
  - [ ] `npm outdated` (оценить обновления)
- [ ] Быстрый smoke-test эндпоинтов:
  - [ ] `/api/health`
  - [ ] `/api/locations`
- [ ] Проверка базы:
  - [ ] `SELECT COUNT(*) FROM locations;` (ожидаемо 29+)

### Раз в месяц (30–60 минут)
- [ ] Ревизия `docs/API_CONTRACT.md` (совпадает ли с реальным API)
- [ ] Ревизия `docs/WORKFLOW.md` (правила Codex соблюдаются?)
- [ ] Проверка индексов/планов запросов (если стало “медленно”):
  - [ ] `EXPLAIN ANALYZE` для ключевых запросов (ads feed / my ads)

### Перед релизом / деплоем (каждый раз)
- [ ] Список .env-переменных зафиксирован в `.env.example`
- [ ] Healthcheck проходит на целевом окружении
- [ ] Бэкап базы (dump) сделан
- [ ] Проверка CORS/HTTPS (если фронт отдельно)

---

## 6) “Definition of Done” для каждой задачи
- [ ] Изменены только нужные файлы (без “перелопачивания”)
- [ ] Есть проверка (curl / SQL / небольшой тест)
- [ ] Обновлены docs (если меняли API/DB)
- [ ] Сделан коммит с понятным сообщением

---

## Текущий статус
- ✅ MVP база готова
- ✅ Backend skeleton готов
- ✅ Locations + seed готовы
- ⏭ Next: Locations cascade endpoints → Auth → Ads
